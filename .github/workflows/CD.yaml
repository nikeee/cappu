name: CD

on:
  push:
    branches:
      - main

jobs:
  test:
    name: Test
    runs-on: ubuntu-slim

    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: latest
          cache: npm

      - run: npm ci --no-audit
      - run: node --run ci
      - run: node --run build

      - uses: actions/upload-artifact@v7
        with:
          name: cappu-linux-x64
          path: dist/linux-x64/cappu
      - uses: actions/upload-artifact@v7
        with:
          name: cappu-linux-arm64
          path: dist/linux-arm64/cappu
      - uses: actions/upload-artifact@v7
        with:
          name: cappu-darwin-x64
          path: dist/darwin-x64/cappu
      - uses: actions/upload-artifact@v7
        with:
          name: cappu-darwin-arm64
          path: dist/darwin-arm64/cappu
      - uses: actions/upload-artifact@v7
        with:
          name: cappu-windows-x64
          path: dist/windows-x64/cappu.exe

  image:
    name: Image
    runs-on: ubuntu-latest
    needs: test

    permissions:
        contents: read
        packages: write

    steps:
      - uses: docker/setup-qemu-action@v4
      - uses: docker/setup-buildx-action@v4

      - id: meta
        uses: docker/metadata-action@v6
        with:
          images: "ghcr.io/nikeee/cappu"
          tags: |
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - uses: docker/login-action@v4
        if: ${{ github.event_name != 'pull_request' }}
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v7
        with:
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64,linux/arm64
