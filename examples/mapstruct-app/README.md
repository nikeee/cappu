# mapstruct-app

MapStruct's annotation processor (resolved into `.cappu/lib/processors`)
generates the mapper implementation during `cappu compile` (#7):

```sh
cappu install
cappu compile
java -jar dist/mapstruct-app.jar
```
