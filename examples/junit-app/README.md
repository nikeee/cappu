# junit-app

`cappu test` compiles `src/test/java` and runs the JUnit Platform console
launcher over it (#16):

```sh
cappu install                 # junit-jupiter into .cappu/lib/test-classes
cappu test
```
