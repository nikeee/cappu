# gson-app

A one-file project using Gson from Maven Central. Build the fat jar and run it:

```sh
cappu install                 # downloads gson into .cappu/lib/classes
cappu compile                 # builds the fat jar into dist/
java -jar dist/gson-app.jar
```
