# Run lifecycle scripts in this repo (overrides a stricter global
# ignore-scripts). Lets `npm version` fire the `version` hook that syncs
# togo/internal/meta/meta.go - see package.json scripts.version.
ignore-scripts=false
