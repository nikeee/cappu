package p;

class T {
    void m() {
        String s =
                "a1,b1\n" + // 1)
                        "a2,b2\n" + // 2)
                        "a3,b3\n"; // 3)
        String t =
                "a1,b1\n" // 1)
                        + "a2,b2\n" // 2)
                        + "a3,b3\n"; // 3)
        String u =
                "AaEeIiOoUu"
                        + // grave
                        "AaEeIiOoUuYy"
                        + // acute
                        "AaOoNn"; // tilde
        String v =
                "unescape(String) failed"
                        + ": expected '"
                        + escapeJava(expected)
                        +
                        // we escape this so we can see it in the error message
                        "' actual '"
                        + escapeJava(actual);
    }
}
