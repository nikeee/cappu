package p;

class T {
    private /* not final might not matter for the test. */ AtomicInteger value;

    void m() {
        final Object /* Stream<String> */ resourcesStream = compute();
        for (final Object /* ModuleLayer */ layer : layerOrderFinal) {
            use(layer);
        }
    }

    void n(final /* nullable */ String s, int /* count */ n) {}
}
