package p;

class C {
  void m(String theInputLine) {
    Matcher theMatcherValue =
        theListItemStartPattern.matcher(theInputLineThatIsBeingMatchedAgainstThePattern);
    theResultFieldValue =
        someHelperObjectInstance.computeTheResultUsing(firstArgumentValue, theSecondArgument);
  }
}
